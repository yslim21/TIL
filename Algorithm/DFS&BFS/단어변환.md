# 단어 변환

[단어 변환](https://school.programmers.co.kr/learn/courses/30/lessons/43163)

## 개념
+ dog -> pog, dig등 문자 한개 변할 수 있는 방법이 여러개므로 목적 문자까지 변하는 최단거리를 구해햐함 = bfs 이용
+ set에 words array 저장
+ queue에 시작 단어 넣기
  + 반복문 돌면서 
    + 현재단어에서 바뀔 수 있는 단어 모두 queue에 넣기 
    + 넣은 단어들에서 또 바뀔수 있는 모든 단어 탐색 
    + 꺼낸 단어가 target이랑 일치하면 종료 

## 풀이
```java
import java.util.*;
import java.io.*; 

class Solution {
    public int solution(String begin, String target, String[] words) {
        int answer = 0;
        Queue<String> q = new LinkedList<>(); 
        Set<String> set = new HashSet<>(Arrays.asList(words));
        
        if(!set.contains(target)){
            return 0; 
        }
        
        q.offer(begin);
        
        while(true){
//             String cur = q.poll();
            
//             if (cur.equals(target)){
//                 return answer; 
//             }
            
//             for (String word : set.toArray(new String[set.size()])){
//                 // 변환 가능하면 queue에 해당 단어를 추가하고 set에서 제거
//                 if(canConvert(cur,word)){
//                     q.offer(word);
//                     set.remove(word);
//                     answer++; 
//                 }
//             }
//             if(q.isEmpty()){
//                 return 0; 
//             }
//         }
        
//         //return 0;
            for (int i=0; i<q.size(); i++){
                // queue에서 단어를 하나씩 꺼내어 current (현재단어)에 저장
                String current = q.poll();
                
                if (current.equals(target)){
                    return answer; // 타겟 단어에 도달
                }
                
                // HashSet 타입은 for 루프를 사용할 수 없으므로 to.Array()를 사용해 배열로 변환
                for (String word : set.toArray(new String[set.size()])){
                    // 변환 가능하면 queue에 해당 단어를 추가하고 set에서 제거
                    if(canConvert(current,word)){
                        q.offer(word);
                        set.remove(word);
                    }
                }
            }
            
            answer++;
        }
    }
    
    private boolean canConvert(String word1, String word2){
        int diffCnt = 0;
        for (int i = 0; i<word1.length(); i++){
            if (word1.charAt(i) != word2.charAt(i)){
                diffCnt++;
            }
        }
        if(diffCnt ==1){
            return true; 
        }else{
            return false; 
        }
    }
}
```